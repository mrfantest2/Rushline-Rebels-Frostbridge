# Rushline Binary Distribution

This folder is a distribution-only channel for the separate private `mrfantest2/Rushline` source repository.

It contains only public update metadata and may reference compiled APK release assets. Rushline source code is not mirrored here.

The manifest at `latest.json` is consumed by the Rushline in-app updater.
