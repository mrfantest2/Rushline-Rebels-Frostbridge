# Task 1 — Runtime, Protocol, Validation

Implemented:

- Node.js 22 runtime manifest
- Express/Socket.IO dependency declaration
- protocol version, event names, stable error codes, canonical character IDs
- production default settings and process configuration
- room/name/character/move/settings validators
- Node built-in validator test suite
- temporary CI bootstrap that installs dependencies, runs tests, and uploads the generated lockfile

Verification target: GitHub Actions on the production pull request.
